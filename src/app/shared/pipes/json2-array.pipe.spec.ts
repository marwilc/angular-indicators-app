import { Json2ArrayPipe } from './json2-array.pipe';

describe('Json2ArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new Json2ArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
